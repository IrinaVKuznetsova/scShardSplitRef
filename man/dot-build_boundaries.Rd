% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0X_determine_split_regions.R
\name{.build_boundaries}
\alias{.build_boundaries}
\title{Generate split boundaries for an interval}
\usage{
.build_boundaries(start, end, limit)
}
\arguments{
\item{start}{Integer: interval start (inclusive).}

\item{end}{Integer: interval end (exclusive).}

\item{limit}{Integer: maximum width per chunk in base pairs.}
}
\value{
Integer vector of boundary positions. Empty if interval width <=
limit.
}
\description{
Calculates interior boundary points that divide a coordinate interval
[start, end) into chunks of approximately equal size, each at most \code{limit}
base pairs wide.
}
\details{
The function calculates the number of chunks needed, then generates
evenly-spaced boundaries. Interior boundaries are rounded to the nearest
integer. The first and last boundaries are excluded (they are the interval
endpoints).
}
\examples{
\dontrun{
  boundaries <- .build_boundaries(0L, 1000L, 250L)
  # Returns something like: 250, 500, 750
}

}
