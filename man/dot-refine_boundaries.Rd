% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0X_determine_split_regions.R
\name{.refine_boundaries}
\alias{.refine_boundaries}
\title{Iteratively refine boundaries}
\usage{
.refine_boundaries(
  boundaries,
  gene_ranges,
  chr_end,
  limit,
  shift_by,
  max_iter = 100L
)
}
\arguments{
\item{boundaries}{Integer vector: initial boundary positions.}

\item{gene_ranges}{Data frame with merged gene ranges (columns 'start',
'end').}

\item{chr_end}{Integer: chromosome end coordinate.}

\item{limit}{Integer: maximum allowed interval width.}

\item{shift_by}{Integer: base pairs to shift right when inside a gene.}

\item{max_iter}{Integer: maximum number of iterations. Defaults to 100L.}
}
\value{
Integer vector: refined boundary positions after convergence or
max iterations.
}
\description{
Core algorithm that repeatedly adjusts boundaries by shifting past genes
and subdividing oversized intervals until convergence.
}
\details{
Each iteration:
\enumerate{
\item Shifts all boundaries past overlapping genes
\item Collects extra boundaries from oversized intervals
\item Deduplicates and filters boundaries
\item Shifts again
\item Checks for convergence (identical to previous iteration)
}
}
\examples{
\dontrun{
  refined <- .refine_boundaries(
    c(100L, 200L), gene_ranges, 300L, 50L, 1L
  )
}

}
