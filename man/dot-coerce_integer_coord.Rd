% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_inputs.R
\name{.coerce_integer_coord}
\alias{.coerce_integer_coord}
\title{Coerce a coordinate column to integer, safely}
\usage{
.coerce_integer_coord(x, label = "coordinate")
}
\arguments{
\item{x}{Numeric vector (integer, double, or character coercible to
numeric) of coordinate values.}

\item{label}{Character scalar used in error messages to identify which
column failed (e.g. \code{"RegionStart"}).}
}
\value{
Integer vector, same length as \code{x}.
}
\description{
Genomic coordinates must always be whole-number \code{integer}, never
\code{double}. Plain \code{as.integer()} is not safe to use directly on trusted
input because it silently returns \code{NA} (with only a warning) for values
outside \R's 32-bit integer range, and silently truncates fractional
values. This helper fails loudly instead in both cases so a type problem
is caught immediately at the point of reading, rather than surfacing much
later as a scientific-notation string or a mysteriously missing row.
}
\examples{
\dontrun{
  .coerce_integer_coord(c(0, 100000000), "RegionStart")
  .coerce_integer_coord(3.5, "RegionStart") # errors: not whole numbers
}

}
\keyword{internal}
