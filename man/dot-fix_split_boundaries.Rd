% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine_split_regions.R
\name{.fix_split_boundaries}
\alias{.fix_split_boundaries}
\title{Fix split boundaries to avoid genes}
\usage{
.fix_split_boundaries(
  split_df,
  gene_ranges,
  limit,
  shift_by = 1L,
  clearance = 1L
)
}
\arguments{
\item{split_df}{Data frame with columns 'chr', 'start', 'end' representing
initial split intervals.}

\item{gene_ranges}{Data frame with already-merged, non-overlapping gene
ranges for the same chromosome (columns 'start', 'end'), as produced by
\code{\link[=.merge_all_gene_ranges]{.merge_all_gene_ranges()}}. Passed in pre-merged so this function does
not redo the sort-and-merge work for every region on the chromosome.}

\item{limit}{Integer: maximum allowed interval width in base pairs.}

\item{shift_by}{Integer: step size (bp) used to walk boundaries rightward
when clearance is violated. Defaults to 1L.}

\item{clearance}{Integer: minimum required clearance (bp) from nearby genes
on both sides. Defaults to 1L.}
}
\value{
Data frame with same structure as \code{split_df} but with adjusted
boundaries that satisfy the size and gene-avoidance constraints.
}
\description{
Adjusts interval boundaries to ensure none fall within gene annotations.
Iteratively shifts boundaries past genes and subdivides oversized intervals
until all constraints are satisfied.
}
\details{
The algorithm iteratively:
\enumerate{
\item Shifts boundaries rightward in \code{shift_by} bp increments until they
satisfy the \code{clearance} gene distance on both sides
\item Subdivides intervals exceeding the limit
\item Shifts again after subdivision
\item Repeats until convergence
}

Raises an error if constraints cannot be satisfied.
}
\examples{
\dontrun{
  fixed_df <- fix_split_boundaries(
    split_df, gene_ranges, limit = 50000, shift_by = 1, clearance = 1
  )
}

}
\keyword{internal}
