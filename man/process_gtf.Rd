% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_process_gtf.R
\name{process_gtf}
\alias{process_gtf}
\title{Process a GTF file by assigning split-region sequence names}
\usage{
process_gtf(
  split_regions_bed,
  gtf,
  genome_name,
  genome_version,
  keep_attributes = NULL,
  out_path = "."
)
}
\arguments{
\item{split_regions_bed}{Character: File path to a BED-like file
containing split intervals as produced by \code{\link[=determine_split_regions]{determine_split_regions()}}.
The first three columns must be: \code{Chr}, \code{RegionStart}, \code{RegionEnd}.}

\item{gtf}{Path to a GTF file containing gene annotations.
Must contain standard 9 GTF columns. Comments starting with '#' are
ignored.}

\item{genome_name}{Genome identifier used in the output filename.}

\item{genome_version}{Genome version string used in the output filename.}

\item{keep_attributes}{Character vector of attribute keys to keep (e.g.,
\code{c("gene_id", "transcript_id", "gene_name")}). If provided, the GTF
attribute column will be filtered to keep only these fields. Defaults to
\code{NULL} (no filtering).}

\item{out_path}{Directory where the output GTF will be written. Defaults to
the current working directory (\code{"."}).}
}
\value{
The function writes the filtered GTF file to disk
at \code{out_path}. Hardcoded file name \link{IK --edit}
}
\description{
Reads a BED-like "split regions" file and a GTF, assigns each GTF feature to
exactly one split-region interval, and replaces the GTF \code{Chr} field with a
split-region name of the form \code{Chr-RegionStart-RegionEnd}.
}
\details{
A feature is considered inside a region when:
\itemize{
\item \code{start >= RegionStart}
\item \code{start <  RegionEnd}
\item \code{end   <= RegionEnd}
}

The function aborts if:
\itemize{
\item any GTF feature matches more than one region (overlapping regions), or
\item any GTF feature matches no region (regions don't fully cover the GTF).
}
}
\examples{
# Minimal working example using temporary files

  reg_file <- system.file("extdata",
                   "IN0_toy_centromeres_for_gtf.bed",
                   package = "scShardSplitRef",
                   mustWork = TRUE)
  gtf_file <- system.file("extdata",
             "A3_toy_all_scenarios_2chr.gtf",
             package = "scShardSplitRef",
             mustWork = TRUE)

  process_gtf(
    split_regions_bed = reg_file,
    gtf = gtf_file,
    genome_name = "Example",
    genome_version = "v1.0",
    out_path = "."
  )

}
