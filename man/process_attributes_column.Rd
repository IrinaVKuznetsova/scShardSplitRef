% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_process_attributes_column_FUN3.r
\name{process_attributes_column}
\alias{process_attributes_column}
\title{Filter and write the GTF attribute column}
\usage{
process_attributes_column(
  result,
  keep_attributes,
  out_path = ".",
  genome_name,
  genome_version
)
}
\arguments{
\item{result}{A data frame from \code{process_gtf()} containing standard GTF
columns including \code{attribute}.}

\item{keep_attributes}{Character vector of attribute keys to keep, e.g.
\code{c("gene_id", "transcript_id", "gene_name")}.}

\item{out_path}{Directory where the output GTF will be written. Defaults to
the current working directory (\code{"."}).}

\item{genome_name}{Genome identifier used in the output filename.}

\item{genome_version}{Genome version string used in the output filename.}
}
\value{
The function writes the filtered GTF file to disk
at \code{out_path}.
}
\description{
Keeps only the requested keys from the 9th GTF column (\code{attribute}) and
writes a final tab-delimited GTF file.
}
\examples{
# keep_attributes <- c("gene_id", "transcript_id", "gene_name")
# final_gtf_df <- process_attributes_column(
#   result = processed_gtf,
#   keep_attributes = keep_attributes,
#   genome_name = "Rye_Lo7",
#   genome_version = "v1p1p1"
# )
}
