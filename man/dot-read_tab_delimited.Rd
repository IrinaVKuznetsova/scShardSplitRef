% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_inputs.R
\name{.read_tab_delimited}
\alias{.read_tab_delimited}
\title{Shared \code{read.table()} wrapper for TAB-delimited genomic files}
\usage{
.read_tab_delimited(file = NULL, text = NULL, comment_char = "#", fill = FALSE)
}
\arguments{
\item{file}{Character: path to read from. Exactly one of \code{file}/\code{text}
must be supplied.}

\item{text}{Character vector of already-read lines to parse via a text
connection. Exactly one of \code{file}/\code{text} must be supplied.}

\item{comment_char}{Character: comment-line prefix, passed to
\code{read.table()}'s \code{comment.char}. Defaults to \code{"#"}, matching
\code{read.table()}'s own built-in default (several callers below rely on
this default implicitly, exactly as they relied on \code{read.table()}'s
default before this refactor). Use \code{""} to disable comment stripping.}

\item{fill}{Logical: passed to \code{read.table()}'s \code{fill}. \code{TRUE} pads
ragged (short) rows with \code{NA} instead of erroring. Defaults to \code{FALSE}.}
}
\value{
Data frame, exactly as returned by \code{\link[utils:read.table]{utils::read.table()}}.
}
\description{
All of the package's BED/GTF readers (\code{\link[=.read_bed_file]{.read_bed_file()}},
\code{\link[=.read_gtf_file]{.read_gtf_file()}} in \code{determine_split_regions.R};
\code{\link[=.read_and_validate_bed]{.read_and_validate_bed()}}, \code{\link[=.read_and_validate_gtf]{.read_and_validate_gtf()}} here) call
\code{utils::read.table()} with the same core options
(\code{sep = "\\t"}, \code{header = FALSE}, \code{quote = ""}, \code{stringsAsFactors = FALSE}), differing only in whether they read from a file path or
already-read lines, whether comments are stripped, and whether ragged
rows are padded. This factors out just that repeated call.
}
\details{
Deliberately narrow in scope: each caller keeps its own file-exists
check, column-count check, column naming, and error messages exactly as
before, so this changes no external behavior, message, or signature --
only the duplicated \code{read.table()} invocation itself.
}
\keyword{internal}
