% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_validate_inputs.R
\name{validate_inputs}
\alias{validate_inputs}
\title{Validate BED and GTF files}
\usage{
validate_inputs(bed, gtf)
}
\arguments{
\item{bed}{Path to a 3-column BED-like file (chr, start, end).
Columns should be TAB-delimited with chromosome name, region start
(0-based), and region end coordinates.}

\item{gtf}{Path to a GTF file containing gene annotations.
Must contain standard 9 GTF columns. Comments starting with '#' are
ignored.}
}
\value{
List with named elements:
\itemize{
\item \code{bed}: Data frame with at least 3 BED columns plus any
additional columns from the input file.
\item \code{gtf}: Data frame with 9 GTF columns
}
}
\description{
Reads and validates BED and GTF files, checking for:
\itemize{
\item File existence
\item TAB-delimited format with correct number of columns
\item Valid BED coordinate ranges (start < end)
}
}
\details{
Performs four checks in sequence:
\enumerate{
\item Both files exist
\item GTF has >= 9 columns
\item BED has >= 3 columns
\item All BED ranges satisfy RegionStart < RegionEnd
}
}
\examples{
  regions <- system.file("extdata/AlgorithmToy.bed",
                   package = "scShardSplitRef",
                   mustWork = TRUE)

  genes <- system.file("extdata/AlgorithmToy.gtf",
             package = "scShardSplitRef",
             mustWork = TRUE)

  result <- validate_inputs(regions, genes)

  bed <- result$bed
  gtf <- result$gtf

  gtf
  bed

}
\keyword{internal}
