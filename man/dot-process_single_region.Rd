% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine_split_regions.R
\name{.process_single_region}
\alias{.process_single_region}
\title{Process a single genomic region}
\usage{
.process_single_region(region, genes, limit, shift_by, clearance = 1L)
}
\arguments{
\item{region}{Data frame row with columns 'chr', 'start', 'end'.}

\item{genes}{Data frame with gene annotations including 'chr', 'start',
'end'.}

\item{limit}{Integer: maximum allowed interval width in base pairs.}

\item{shift_by}{Integer: step size (bp) used to walk boundaries rightward
when clearance is violated.}

\item{clearance}{Integer: minimum required clearance (bp) from nearby genes
on both sides.}
}
\value{
Data frame with columns 'chr', 'start', 'end' representing the
processed split intervals for this region.
}
\description{
Splits a single genomic region according to the size limit and adjusts
boundaries to avoid genes. If the region has a non-zero start (centromere),
it is split into two parts: before and after the centromere.
}
\details{
If the region start is > 0, the function assumes this marks a centromere
and splits the region into [0, start) and [start, end). Each part is
independently split according to the size limit. Boundaries are then
adjusted to avoid genes in the region.
}
\examples{
\dontrun{
  region <- data.frame(chr = "chr1", start = 100000, end = 200000)
  genes_subset <- genes[genes$chr == "chr1", ]
  result <- .process_single_region(region, genes_subset,
                                   limit = 50000,
                                   shift_by = 1,
                                   clearance = 1)
}

}
\keyword{internal}
