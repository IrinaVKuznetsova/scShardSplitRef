% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/determine_split_regions.R
\name{.assert_feasible_limit}
\alias{.assert_feasible_limit}
\title{Assert that the size limit is feasible}
\usage{
.assert_feasible_limit(merged_gene_ranges, limit, clearance)
}
\arguments{
\item{merged_gene_ranges}{Named list, keyed by chromosome, of already
merged gene ranges as produced by \code{\link[=.merge_all_gene_ranges]{.merge_all_gene_ranges()}}.}

\item{limit}{Integer: requested maximum interval width.}

\item{clearance}{Integer: minimum required clearance (bp) from nearby genes
on both sides.}
}
\value{
Invisible TRUE if feasible. Raises error with remediation advice
if not.
}
\description{
Checks that the requested size \code{limit} is larger than any single gene
with two-sided boundary clearance. Raises an informative error if not
feasible.
}
\details{
Checks if any merged range plus two-sided clearance (2 * \code{clearance})
exceeds the limit. Reports the worst (largest) blocking interval and
suggests an appropriate minimum limit value.
}
\examples{
\dontrun{
  .assert_feasible_limit(merged_gene_ranges, limit = 50000, clearance = 1)
}

}
\keyword{internal}
